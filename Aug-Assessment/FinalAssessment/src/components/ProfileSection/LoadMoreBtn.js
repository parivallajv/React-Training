import { LoadMoreDivision, LoadMoreFont } from "../../styles";

const LoadMoreBtn = () => {
  return (
    <LoadMoreDivision>
      <LoadMoreFont>+5 more</LoadMoreFont>
    </LoadMoreDivision>
  );
};

export default LoadMoreBtn;
